# iCTS Test Layout

This folder mirrors `source/` modules to keep test coverage organized by module
and functionality. Tests must use synthetic data only and keep results
reproducible via explicit random seeds.

## Output

Set `ICTS_TEST_OUTPUT_DIR` to control where SVGs and logs are written.
Default: `icts_test_output` under the current working directory.
